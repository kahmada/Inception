🎯 Objectif du projet Inception (en bref)
Créer une mini-infrastructure web composée de plusieurs services conteneurisés (via Docker),
 configurés manuellement (pas d'images toutes faites comme nginx:latest), avec un focus sur :

Sécurité (TLS, variables d'environnement, secrets)

Isolation (1 service = 1 conteneur)

Configuration personnalisée

Utilisation de Docker volumes pour la persistance

Tu dois tout construire depuis zéro, en apprenant à :

Utiliser Docker & Docker Compose

Configurer des services web réels (MariaDB, WordPress, NGINX)

Gérer le réseau, le stockage, la sécurité

🔗 Relation entre MariaDB, WordPress et NGINX
Voici comment ces trois services interagissent entre eux :

1. MariaDB (base de données SQL)
C’est la base de données utilisée par WordPress.

Elle stocke les articles, les utilisateurs, les commentaires, les paramètres du site, etc.

Elle doit être sécurisée (mot de passe root, base de données dédiée à WordPress, etc.).

WordPress se connecte à MariaDB via un nom d’hôte Docker (par exemple mariadb:3306).

2. WordPress (site web dynamique)
C’est le CMS (Content Management System) que l'utilisateur final utilisera.

Il a besoin d’une base de données (MariaDB) pour fonctionner.

Il s'exécute avec PHP-FPM (interpréteur PHP), dans un conteneur à part.

Il communique avec MariaDB pour lire/écrire des données.

3. NGINX (serveur web)
C’est le point d’entrée HTTP/HTTPS de ton infrastructure.

Il agit comme reverse proxy vers WordPress (php-fpm) via fastcgi_pass.

Il gère aussi le certificat SSL (TLS) si tu veux activer HTTPS.

C’est NGINX qui répond aux requêtes du navigateur, puis transmet à WordPress/PHP.

📦 Exemple de fonctionnement :
plaintext
Copy
Edit
Client navigateur (Chrome) ---> NGINX (HTTPS) ---> WordPress (PHP-FPM) ---> MariaDB
L’utilisateur accède à ton site en HTTPS (NGINX).

NGINX transfère la requête à WordPress (PHP).

WordPress interroge la base de données MariaDB pour afficher les données.

WordPress renvoie la page à NGINX, qui la transmet au client.


------->mariadb : son role c est de Stocker les données WordPress et il est conecter a wordpress 

------->wordpress : son role est de Gérer le contenu, logique du site  et il est conecter a mariadb et nginx 

------->nginx : son role est Servir le site en HTTPS et il se conecte a wordpress  et le client 



**************************************************************************************************888
WordPress
Un CMS (Content Management System) : il permet de créer un site sans coder.

WordPress utilise :

PHP côté serveur

MySQL/MariaDB pour stocker les données

⚙️ PHP-FPM (FastCGI Process Manager)
PHP-FPM est un moteur d'exécution PHP.
Il écoute sur un socket Unix ou un port TCP, et exécute les fichiers PHP quand Nginx le lui demande.

🔁 Nginx ne sait pas exécuter PHP, il transmet donc les fichiers .php à PHP-FPM via FastCGI.