[www]#Dans Docker, ce pool va gérer toutes les requêtes PHP venant de Nginx, dans le conteneur wordpress.
user = www-data# Cela signifie que les processus PHP s'exécutent en tant qu'utilisateur www-data, ce qui est standard et sécurisé pour les serveurs web.
group = www-data
listen = 9000
#👉 Très important dans Inception : PHP-FPM écoute sur le port 9000 dans le conteneur wordpress.

#Le conteneur Nginx (dans nginx) doit envoyer les requêtes PHP vers wordpress:9000, grâce au réseau Docker inception_network.
pm = dynamic#PHP-FPM gère dynamiquement les processus selon la charge. C’est adapté à Docker, car ça évite d’utiliser trop de RAM inutilement.
pm.max_children = 5#Le nombre maximum de processus PHP pouvant être lancés.
#Si tu as peu de RAM dans ta VM ou ton hôte Docker, c’est bien de limiter.


pm.start_servers = 2#Lance 2 processus PHP au démarrage.
pm.min_spare_servers = 1#Garde entre 1 et 3 processus "en attente" pour répondre rapidement.
pm.max_spare_servers = 3
#Dans le projet Inception, tu construis un service WordPress qui utilise :

#Nginx pour servir le site.

#PHP-FPM pour exécuter le code PHP.

#MariaDB pour stocker les données du site.

#Ton conteneur wordpress contient un serveur PHP-FPM configuré pour communiquer avec Nginx. Ce serveur utilise un fichier comme www.conf pour déterminer comment il fonctionne en interne.



#| Élément             | Rôle dans Inception                                          |
#| ------------------- | ------------------------------------------------------------ |
#| `listen = 9000`     | Permet à Nginx (dans un autre conteneur) de parler à PHP-FPM |
#| `user = www-data`   | Assure que PHP a les bons droits sur les fichiers WordPress  |
#| `pm = dynamic`      | Adapte automatiquement la charge du conteneur `wordpress`    |
#| `max_children` etc. | Évite de surcharger la machine hôte avec trop de processus   |
